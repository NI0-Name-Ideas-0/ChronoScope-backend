cors:
  allowed-origins:
    - https://chronoscope.ni0.team

spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/chronoscope
    username: chronoscope
    password: uze$AQbLtP79Z4
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect